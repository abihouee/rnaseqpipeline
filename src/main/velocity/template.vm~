OUTDIR=../align
##SAMPLES=30VGKM1 30VGKM2 

PHONY=all clean
##all: $(addsuffix _fastQC,$(SAMPLES))
all: #foreach($sample in $config) ${sample.name} #end

#foreach($record in $config)

$(addsuffix _fastQC,${record.name}): $(wildcard $(INDIR)/${record.name}*.fastq.gz)
mkdir -p $(OUTDIR)/fastQC/${record.name}/ && \
$(FASTQC) --casava -o $(OUTDIR)/fastQC/${record.name} $^ 

#end
